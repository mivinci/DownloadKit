name: CI

# Cancel in-progress runs for the same PR / branch on new pushes
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main]
    paths:
      - '**.c'
      - '**.h'
      - '**.cpp'
      - '**.hpp'
      - '**/CMakeLists.txt'
      - '**.cmake'
      - '.github/workflows/ci.yml'
  pull_request:
    branches: [main]
    paths:
      - '**.c'
      - '**.h'
      - '**.cpp'
      - '**.hpp'
      - '**/CMakeLists.txt'
      - '**.cmake'
      - '.github/workflows/ci.yml'

jobs:
  # ── Build & Test ────────────────────────────────────────────────────────
  test:
    name: Test (${{ matrix.os }} / TLS=${{ matrix.tls_backend }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        tls_backend: [openssl, mbedtls]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ── Dependencies ──────────────────────────────────────────────────────

      - name: Install dependencies (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtest-dev libnghttp2-dev \
            libssl-dev libmbedtls-dev libcjson-dev libusrsctp-dev

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install googletest llhttp nghttp2 openssl mbedtls libusrsctp cjson

      # ── Cache ─────────────────────────────────────────────────────────────

      # Cache key is prefixed with a version tag. Bump it whenever
      # something outside CMakeLists.txt invalidates old build dirs —
      # most notably the repo rename xKit → moo, which changed the
      # checkout path on the runner from /Users/runner/work/xKit/xKit
      # to /Users/runner/work/moo/moo. CMakeCache.txt records absolute
      # paths, so any cache produced before the rename makes cmake
      # bail out with "source does not match". The v2 tag below
      # retires all pre-rename entries.
      - name: Cache build directory
        uses: actions/cache@v4
        with:
          path: build
          key: build-v2-${{ matrix.os }}-${{ matrix.tls_backend }}-${{ hashFiles('**/CMakeLists.txt') }}
          restore-keys: |
            build-v2-${{ matrix.os }}-${{ matrix.tls_backend }}-

      # ── Run test script ───────────────────────────────────────────────────

      - name: Run tests (Linux)
        if: runner.os == 'Linux'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: |
          bash scripts/test-linux.sh \
            --ci --asan \
            --base-sha "$BASE_SHA" \
            -t ${{ matrix.tls_backend }} \
            -j "$(nproc)" \
            -B build

      - name: Run tests (macOS)
        if: runner.os == 'macOS'
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: |
          zsh scripts/test-mac.sh \
            --ci --asan \
            --base-sha "$BASE_SHA" \
            -t ${{ matrix.tls_backend }} \
            -j "$(sysctl -n hw.ncpu)" \
            -B build

  # ── Branch naming lint ────────────────────────────────────────────────────

  lint-branch:
    name: Lint branch name
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Check branch naming convention
        env:
          BRANCH: ${{ github.head_ref }}
        run: |
          echo "Branch: $BRANCH"
          if echo "$BRANCH" | grep -qE '^(main|moo\/|qclaw\/|codebuddy\/|workbuddy\/|claude\/|opencode\/|renovate/)'; then
            echo "✅ Branch name OK"
          else
            echo "❌ Branch name '$BRANCH' does not follow convention."
            echo "   Expected prefix: moo/, qclaw/, codebuddy/, workbuddy/, claude/ or opencode/"
            exit 1
          fi
