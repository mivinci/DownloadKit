
# 2026-03-30 日报

## 今日工作

### 1. malloc 测试 PR 合入

**内容**：PR #6 合入 main，包含昨天编写的内存管理单元测试

### 2. Task 模块重构：移除冗余 err 字段

**目标**：简化 `xTask` 结构体，移除不再需要的 `err` 字段（PR #7）

### 3. 日志与回溯功能（xLog + xBacktrace）

**开发过程**：

1. 先实现 `xThrow`（带 `bool fatal` 参数，可选 abort）— PR #8
2. 重命名 `xThrow` → `xLog`，新增 `xBacktrace` 模块 — PR #9
3. 清理遗留的 `throw.h`/`throw.c`/`throw_test.cpp` 文件

**开发内容**：
- `xbase/log.c` / `log.h`：日志输出，支持 fatal 级别自动 abort
- `xbase/backtrace.c` / `backtrace.h`：栈回溯功能（175 行），基于 libunwind
- `cmake/FindLibunwind.cmake`：libunwind 查找模块（46 行）
- `xbase/backtrace_test.cpp`：回溯测试（176 行）

### 4. 模块重命名：malloc → memory

**内容**：将 `xbase/malloc.h`/`malloc.c` 重命名为 `xbase/memory.h`/`memory.c`（PR #10），命名更规范

### 5. 新增 xTaskGroupGlobal

**目标**：提供全局共享的默认任务组（PR #11）

**开发内容**：
- 在 `task.c` 中新增 `xTaskGroupGlobal()` 函数（24 行）
- 新增对应测试（45 行）

### 6. 实现通用最小堆（xHeap）

**目标**：为后续定时器功能提供基础数据结构（PR #12）

**开发内容**：
- `xbase/heap.c`（173 行）：通用最小堆实现
- `xbase/heap.h`（103 行）：公开 API
- `xbase/heap_test.cpp`（217 行）：单元测试

### 7. 实现单调定时器（xTimer）

**目标**：基于最小堆实现高精度单调定时器（PR #13）

**开发内容**：
- `xbase/timer.c`（257 行）：定时器核心实现
- `xbase/timer.h`（119 行）：公开 API
- `xbase/timer_test.cpp`（241 行）：单元测试

**涉及文件**：

| 文件 | 改动说明 |
|------|----------|
| `xbase/task.c` / `task.h` | 移除 err 字段，新增 xTaskGroupGlobal |
| `xbase/log.c` / `log.h` | 日志模块（从 throw 重命名） |
| `xbase/backtrace.c` / `backtrace.h` | 栈回溯模块（新增） |
| `xbase/memory.c` / `memory.h` | 从 malloc 重命名 |
| `xbase/heap.c` / `heap.h` | 通用最小堆（新增） |
| `xbase/heap_test.cpp` | 堆测试（新增，217 行） |
| `xbase/timer.c` / `timer.h` | 单调定时器（新增） |
| `xbase/timer_test.cpp` | 定时器测试（新增，241 行） |
| `cmake/FindLibunwind.cmake` | libunwind 查找模块（新增） |

## 经验总结

1. **命名很重要**：`xThrow` → `xLog` 的重命名说明初始命名应该更慎重，避免后续大面积改名
2. **最小堆是定时器的基石**：O(log n) 的插入和删除，O(1) 的最小值查询，非常适合管理定时器
3. **今天产出很高**：7 个 PR 合入，涵盖日志、回溯、内存重命名、全局任务组、堆、定时器等多个模块
