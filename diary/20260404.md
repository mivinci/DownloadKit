
# 日报 2026-04-04

## 今日工作

### xbuf 模块开发与提交（PR #32）

完成了 xbuf 模块的全部开发工作，包含三种缓冲区实现：

| Header | Type | Description |
| ------ | ---- | ----------- |
| `buf.h` | `xBuffer` | 线性自增长字节缓冲区，2x 扩容策略 |
| `ring.h` | `xRingBuffer` | 固定大小环形缓冲区，power-of-2 掩码索引 |
| `io.h` | `xIOBuffer` | 引用计数块链 I/O 缓冲区（brpc IOBuf 风格），支持零拷贝 split/cut |

#### PoolNode 结构体风格统一

`io.c` 中的内部结构体 `PoolNode` 原来使用手写 `typedef struct`，改为 `XDEF_STRUCT(PoolNode_)` 与项目风格一致。

#### 分支管理与提交

- 创建分支 `codebuddy/xbuf`（经历了 `feat/xbuf-poolnode-style` → `codebuddy/feat-xbuf-module` → `codebuddy/xbuf` 的命名修正）
- 提交 xbuf 模块全部代码（12 个文件，+2464 行）
- 更新 `TODO.md` 和 `README.md` 中的 xbuf 模块描述
- 创建 PR #32：<https://github.com/mivinci/xKit/pull/32>

#### TLS 优化分析（未实施）

分析了 `xIOBuffer` block pool 的 TLS（Thread-Local Storage）优化方案：当前全局 lock-free Treiber stack 在高并发下 CAS 竞争激烈，可加一层 thread-local cache 做批量 transfer，减少原子操作。方案已讨论，暂未实施。

### PR #32 Review 修复与合并

**背景**：xbuf 模块的 PR #32 收到了 owner 的多轮 review，今天集中处理了所有 review 反馈并最终合入 main。

#### 第一轮修复（commit `5a10bae`）

根据初始 review 反馈，修复了以下问题：

1. **`buf_grow()` compact 逻辑 bug**（buf.c）— 原条件 `unread + (needed - unread)` 化简后等于 `needed`，compact 完全不影响判断，简化为 `needed <= buf->cap`
2. **`iobuf_push_ref` nbytes 补偿逻辑统一**（io.c）— 增加 `bool adjust_nbytes` 参数，消除 `xIOBufferCut` 中先加后减的 undo dance
3. **溢出保护**（buf.c / ring.c）— `buf_next_cap` 和 `next_pow2` 增加 `SIZE_MAX` 边界检查
4. **`PoolNode_` 注释**（io.c）— 说明 xIOBlock 被复用为 Treiber stack 节点的语义
5. **EINTR 处理**（buf.c / ring.c / io.c）— 所有 `read`/`write`/`readv`/`writev` 调用加 EINTR 重试
6. **IOV_MAX 感知**（io.c）— `xIOBufferWriteFd` 不再硬编码 64
7. **未使用变量**（ring_test.cpp）— 移除 `WrapAround` 测试中未使用的 `cap`

#### CI 流水线修复（commit `91afd52`）

- **问题**：Ubuntu/GCC 环境下 `SIZE_MAX` 未声明，编译失败
- **根因**：buf.c 和 ring.c 使用了 `SIZE_MAX` 但缺少 `#include <stdint.h>`，macOS/Clang 间接引入了该定义，Linux/GCC 严格模式下不行
- **修复**：两个文件各添加 `#include <stdint.h>`

#### 第二轮 Review 修复（commit `1c03b89`）

reviewer 指出第一轮修复后仍有问题：

1. **`buf_grow()` compact 条件仍是死代码**（buf.c）— `if (needed <= buf->cap)` 和外层检查等价，永远不会走到。改为 `if (unread + needed <= buf->cap)`，正确检查 compact 后空间是否足够
2. **`next_pow2` 注释补充**（ring.c）— 说明为什么返回最大 2 的幂（2^63）而不是 `SIZE_MAX`（ring buffer 依赖 power-of-two 做掩码）
3. **`xIOBlockPoolDrain` 文档化**（io.c）— 原计划加 `assert(refs == 0)`，但发现 `PoolNode_` overlay 会覆盖 `refs` 字段为 `next` 指针，assert 不可行。改为详细文档注释说明 invariant
4. **`xIOBlockPoolWarmup` 初始化**（io.c）— 新增 `blk->refs = 0` 保持一致性

#### 最终合并

所有 review 问题修复完毕，CI 通过，PR #32 合入 main。

### xhttp 模块重构：用 xbuf 替换自定义缓冲区

将 xhttp 模块中自定义的 `xHttpBuf_` 结构体替换为 xbuf 模块的 `xBuffer` API，消除重复的缓冲区实现。

主要改动：

- 移除 `xHttpBuf_` 结构体及 `http_buf_init`/`http_buf_free`/`http_buf_append` 内联函数
- 全部改用 `xBufferCreate`/`xBufferDestroy`/`xBufferAppend`/`xBufferData`/`xBufferLen`/`xBufferConsume`
- `xhttp/CMakeLists.txt` 添加 xbuf 依赖
- SSE parser 中手动 `memmove` compact 替换为 `xBufferConsume`
- `oneshot_on_done` 中手动追加 NUL 终止符以保持 C 字符串兼容

### 修复事件循环 dispatch 中的 use-after-free（xbase）

**问题**：`HttpClientTest.ConcurrentRequests` 测试偶发 Bus error / SEGFAULT（约 60% 复现率）。

**根因**：kqueue 的 `kevent()` 可以在一次调用中返回同一个 fd 的多个事件（如 `EVFILT_READ` + `EVFILT_WRITE`）。当第一个事件的回调触发 curl 的 `CURL_POLL_REMOVE` → `xEventDel` → `sources_remove` 时，`xEventSource_` 被立即 free。但同一批次中后续事件的 `udata` 仍指向已释放的内存，造成 use-after-free。

**修复**：将 `sources_remove` 改为延迟删除模式：

1. `xEventSource_` 新增 `deleted` 标志位
2. `sources_remove` 只标记 `deleted = 1`，不立即 free
3. dispatch 循环中跳过 `deleted` 的 source
4. 循环结束后由 `sources_sweep()` 统一释放已删除的 source

三个后端（kqueue、epoll、poll）均已修复。修复后连续 10+ 次测试全部通过。

### xhttp-use-xbuf 分支提交与 PR #33

将 xhttp 重构、use-after-free 修复、重命名三项改动合并到 `codebuddy/xhttp-use-xbuf` 分支，创建 PR #33：<https://github.com/mivinci/xKit/pull/33>

#### `xEventSources_` → `xEventSourceArray_` 重命名

将 `struct xEventSources_` 重命名为 `struct xEventSourceArray_`，更准确地表达底层动态数组的数据结构语义。同步将所有 `sources_*` 函数重命名为 `source_array_*`：

- `sources_init` → `source_array_init`
- `sources_free` → `source_array_free`
- `sources_add` → `source_array_add`
- `sources_remove` → `source_array_remove`
- `sources_sweep` → `source_array_sweep`
- `sources_find_fd` → `source_array_find_fd`

涉及 `event_private.h`（定义）+ `event_kqueue.c`、`event_epoll.c`、`event_poll.c`（调用点）。

#### CI 流水线修复：unused parameter 警告

PR #33 首次 CI 失败，原因是 `-Wall -Wextra -Werror` 下 `source_array_remove` 的参数 `s` 未使用（改为延迟删除后不再操作数组）。修复：在函数开头加 `(void)s;` 消除警告。

#### 构建系统：本地编译加严格警告

在根 `CMakeLists.txt` 中添加编译标志，使本地构建与 CI 保持一致：

```cmake
add_compile_options(-Wall -Wextra)
add_compile_options($<$<COMPILE_LANGUAGE:C>:-Werror>)
```

- `-Wall -Wextra`：C 和 C++ 都开启
- `-Werror`：仅对 C 文件开启（通过 generator expression 限定），因为 gtest 头文件在 C++ 编译时会产生无法控制的警告

## 涉及文件

| 文件 | 修改内容 |
| ------ | --------- |
| `xbuf/buf.h`, `xbuf/buf.c` | 线性缓冲区实现（含 compact 逻辑修复、溢出保护、EINTR、`<stdint.h>`） |
| `xbuf/ring.h`, `xbuf/ring.c` | 环形缓冲区实现（含溢出保护注释、EINTR、`<stdint.h>`） |
| `xbuf/io.h`, `xbuf/io.c` | 块链 I/O 缓冲区实现（含 nbytes 统一、`PoolNode_` 注释、PoolDrain 文档、IOV_MAX、EINTR、warmup 初始化） |
| `xbuf/buf_test.cpp`, `xbuf/ring_test.cpp`, `xbuf/io_test.cpp` | 单元测试 |
| `xbuf/CMakeLists.txt`, `xbuf/README.md` | 构建配置与模块文档 |
| `CMakeLists.txt`, `TODO.md`, `README.md` | 项目级配置与文档更新 |
| `xhttp/client.c`, `xhttp/client_sse.c` | 替换 `xHttpBuf_` 为 `xBuffer` |
| `xhttp/client_internal.h` | 移除 `xHttpBuf_` 定义，改用 `xBuffer` |
| `xhttp/CMakeLists.txt` | 添加 xbuf 依赖 |
| `xbase/event_private.h` | `xEventSource_` 增加 `deleted` 标志，延迟删除 + sweep；`xEventSources_` → `xEventSourceArray_`，`sources_*` → `source_array_*`；`(void)s` 消除 unused parameter 警告 |
| `xbase/event_kqueue.c` | dispatch 循环跳过 deleted source，结束后 sweep；函数名同步更新 |
| `xbase/event_epoll.c` | 同上 |
| `xbase/event_poll.c` | 同上 |
| `CMakeLists.txt` | 添加 `-Wall -Wextra`（C/C++）和 `-Werror`（C only） |

## 经验总结

1. **compact 逻辑要仔细推导**：`buf_grow()` 的 compact 分支连续两轮 review 都没改对。第一轮把复杂表达式简化为 `needed <= buf->cap`，看似合理但和外层检查等价，仍是死代码。正确做法是 `unread + needed <= buf->cap`——compact 腾出的空间是 `rpos` 之前的部分，需要加上 unread 才是真实的空间需求判断。
2. **overlay 结构要注意字段覆盖**：`PoolNode_` overlay `xIOBlock`时，`next`指针会覆盖`refs`字段。在这种设计下不能对 pool 中 block 的`refs`做 assert 检查，只能通过文档约束 invariant。
3. **跨平台头文件不能依赖间接引入**：`SIZE_MAX` 在 macOS/Clang 下被其他头文件间接引入，但 Linux/GCC 严格模式下必须显式 `#include <stdint.h>`。CI 环境和本地环境的差异要注意。
4. **事件循环 dispatch 中不能立即释放 source**：kqueue/epoll 一次 `kevent()`/`epoll_wait()` 可能返回同一个 fd 的多个事件，如果回调中删除了 source 并立即 free，后续事件的 `udata` 就是悬空指针。正确做法是延迟删除——标记 + sweep。

---

## 晚间工作

### xhttp server 模块开发（PR #44）

在 `codebuddy/xhttp-server` 分支上完成了 xhttp HTTP/1.1 server 模块的全部开发。

#### 核心实现（commit `d745a66`）

基于 xbase 事件循环 + llhttp 解析器，实现了完整的 HTTP/1.1 server：

- **非阻塞 I/O**：基于 xbase 的 kqueue/epoll 事件循环
- **路由匹配**：精确路径 + 方法分发，first-match-wins 策略
- **流式响应**：chunked transfer encoding 支持
- **Server-Sent Events (SSE)**：`xhttp_server_event_source` API
- **完整测试套件**：覆盖路由、SSE、流式响应和边界情况

涉及文件：

- `xhttp/server.h` — 公共 API 头文件
- `xhttp/server.c` — 服务端实现
- `xhttp/server_private.h` — 内部结构定义
- `xhttp/server_test.cpp` — 单元测试
- `docs/xhttp/server.md` — 文档

#### 参数路由实现（commit `19d418d`）

为 server 添加了参数化路由匹配（`:param` segments），支持 `/users/:id` 风格的动态路径：

**核心改动**：

| 文件 | 改动 |
| --- | --- |
| `server_private.h` | 新增 `xHttpRouteSegment_`、`xHttpParam_` 结构，`XHTTP_MAX_PARAMS` |
| `server.h` | `xHttpRequest` 增加 `params_` 字段，新增 `xHttpRequestParam()` API |
| `server.c` | `route_parse_segments()` 预解析、`route_match()` 逐段匹配、`xHttpRequestParam()` 参数查找 |
| `server_test.cpp` | 7 个新测试用例 |
| `docs/xhttp/server.md` | 更新文档，添加 RESTful API 示例 |

**测试用例**：

| 测试 | 验证内容 |
| --- | --- |
| `ParamRouteBasic` | `/users/:id` 匹配 `/users/42` |
| `ParamRouteStringId` | `/users/:id` 匹配 `/users/alice` |
| `ParamRouteMultipleParams` | `/users/:id/:action` 匹配 `/users/99/edit` |
| `ParamRouteExtraSegments404` | `/users/42/extra` 不匹配 `/users/:id` → 404 |
| `ParamRouteNonexistentParam` | 查询不存在的参数返回 NULL |
| `StaticRoutePriorityOverParam` | 静态路由 `/users/me` 优先于 `/users/:id` |
| `ParamRouteMethodNotAllowed` | 参数路由也正确返回 405 |

在 Routing 区域顶部加了 **TODO 注释**：如果路由数量成为瓶颈，可升级为 radix tree。

#### README 更新（commit `a129c99`）

- **Modules 表格**：xhttp 描述更新为 "Async HTTP client & server"
- **Prerequisites 表格**：libcurl 说明改为 client，新增 llhttp 行

#### 创建 PR #44

推送 `codebuddy/xhttp-server` 分支，创建 PR #44：[feat(xhttp): add HTTP/1.1 server with routing, SSE and parameterized routes](https://github.com/mivinci/xKit/pull/44)

#### CI 流水线修复（commit `dada971`）

**问题**：PR #44 的 CI 失败，CMake 找不到 llhttp 依赖。

**根因**：`xhttp/CMakeLists.txt` 使用了 `find_package(Llhttp REQUIRED)`，但 macOS 和 Ubuntu runner 上都没有预装 llhttp。

**修复**：修改 `.github/workflows/ci.yml`：

- **macOS**：`brew install` 加上 `llhttp`
- **Ubuntu**：从 `nodejs/llhttp` 源码编译安装（v9.3.1），因为 apt 没有 llhttp 的包

### HTTP/2 方案讨论与 TODO 记录

讨论了后续支持 HTTP/2 的三种方案：

1. **方案 A（推荐）**：协议层抽象 — 引入 `xHttpProto` vtable 接口，隔离 HTTP/1.1 和 HTTP/2 解析逻辑
2. **方案 B**：nghttp2 集成 — HTTP/2 帧解析 + stream 多路复用
3. **方案 C**：TLS 先行 — OpenSSL/BoringSSL + ALPN 协商

建议 A + B 结合，分三步走：协议抽象 → TLS/h2c → nghttp2 集成。

#### 创建 xhttp/TODO.md（commit `6a7a316`）

将 HTTP/2 方案分析写入 `xhttp/TODO.md`，包含：

- 当前架构现状分析（llhttp 与连接管理的耦合）
- 协议处理器接口设计（`xHttpProto` vtable）
- HTTP/2 stream 多路复用结构（`xHttpStream_`）
- HTTP/1.1 vs HTTP/2 对比表
- 三步实施路线图

修复了 TODO.md 的两个 markdownlint 警告（MD032 列表前空行、MD040 代码块语言标识）。

## 涉及文件（晚间新增）

| 文件 | 修改内容 |
| ------ | --------- |
| `xhttp/server.h` | HTTP server 公共 API，含路由注册、请求/响应、SSE、参数路由 |
| `xhttp/server.c` | Server 完整实现：连接管理、llhttp 解析、路由匹配（含参数路由）、响应写入、SSE |
| `xhttp/server_private.h` | 内部结构定义：连接、路由（含段数组）、参数、响应头 |
| `xhttp/server_test.cpp` | 完整测试套件，含参数路由 7 个新用例 |
| `docs/xhttp/server.md` | Server 模块文档，含 RESTful API 示例 |
| `README.md` | Modules 和 Prerequisites 表格更新 |
| `.github/workflows/ci.yml` | CI 添加 llhttp 依赖安装 |
| `xhttp/TODO.md` | HTTP/2 支持路线图 |

## 经验总结（晚间补充）

1. **CI 依赖要同步更新**：新增外部依赖（llhttp）后，必须同步更新 CI workflow，不能只依赖本地环境。macOS 可以 `brew install`，Ubuntu 可能需要源码编译。
2. **参数路由设计要考虑升级路径**：当前用链表线性扫描 + 逐段匹配，简单够用。但 API 设计（`xHttpServerRoute` / `xHttpRequestParam`）已经兼容 radix tree 升级，后续不需要改公共接口。
3. **HTTP/2 支持的关键是协议抽象**：当前 llhttp 和连接管理紧耦合，后续要支持 HTTP/2 必须先做协议层抽象。好在上层 API（路由、SSE、ResponseWriter）设计得足够通用，可以透明支持两种协议。
